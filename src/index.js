import { loadGame } from './game';
// import createRings from './rings';
// import slotElementsSprite from '../assets/slotElements.png';

loadGame();

// const image = new Image();

// image.onload = function() {
//   ctx.drawImage(image, 25, 0, 250, 250, 0, 0, 200, 200);
//   ctx.drawImage(image, 225, 0, 250, 250, 100, 0, 200, 200);
// };
// image.src = slotElementsSprite;

// createRings(ctx);
